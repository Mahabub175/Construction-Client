/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "thumbs.dreamstime.com",
      "genesisapi.moonsgallerysystem.com",
    ],
  },
};

export default nextConfig;
