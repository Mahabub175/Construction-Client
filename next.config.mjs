/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "thumbs.dreamstime.com",
      "api.photohousemagazine.com",
    ],
  },
};

export default nextConfig;
